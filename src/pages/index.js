import React from "react"

import Layout from "../components/layout"

const IndexPage = () => (
  <Layout>
    <div style={{ maxWidth: `80vw`, marginBottom: `1.45rem` }}>
	<h1 style={{ textAlign: 'center' }}>~SourcePoint~</h1>
	<p style={{ textAlign: 'center' }}>Please excuse the empty room, I just moved in...</p>
    </div>
  </Layout>
)

export default IndexPage
